# Round56 S115/S116 最终产物回退诊断与按需重训报告

**生成时间**: 2026-05-31 22:41
**执行模式**: eval-only（最终验证）

---

## 1. 本轮目标

诊断 Round55 报告中出现的异常：
- Round55 的 `diagnose_geo_feature_flow.py` 发现 `distributed_predictions_v159.pkl` 中 S115/S116 scene_v151 有白天成分（mid/low/clear_peak）
- 但 `final_full` 中 S115/S116 scene_v151 报告为 "all night"

**本轮先诊断，不要直接完整重训。根据诊断结果决定执行 eval-only / dashboard-only / geo-refresh --force / full --force。**

---

## 2. 是否直接完整重训

**结论：先诊断，未完整重训**

Round55 的 "all night" 误判原因是诊断脚本读取了 `all` scope（含大量夜间数据），而非 `test` 评估窗口。

---

## 3. S115/S116 各产物层级对比

| 文件 | scope | S115 scene (test 10-14) | S115 gblend_max | S115 pred_mean | S116 scene (test 10-14) | S116 gblend_max | S116 pred_mean |
|------|-------|:--:|:--:|:--:|:--:|:--:|:--:|
| v159 | hours_10_14 | clear_peak/mid/low（无night） | 507 | 有（非0） | clear_peak/mid/low（无night） | 504 | 有（非0） |
| round36_final | test_10_14 | mid/clear_peak/low（无night） | 828 | 3.19 | mid/clear_peak/low（无night） | 836 | 10.06 |
| round36_eval | test_10_14 | mid/clear_peak/low（无night） | 828 | 3.19 | mid/clear_peak/low（无night） | 836 | 10.06 |
| **canonical_full** | **test_10_14** | **mid/clear_peak/low（无night）** | **828** | **3.19** | **mid/clear_peak/low（无night）** | **836** | **10.06** |
| **canonical_eval** | **test_10-14** | **mid/clear_peak/low（无night）** | **828** | **1.59** | **mid/clear_peak/low（无night）** | **836** | **4.91** |

**结论：所有产物层级均正常，无回退。**

---

## 4. 异常断点

**无异常断点**。

Round55 的误判原因是诊断脚本 bug（读取 all scope 含夜间数据），不是实际产物问题。

---

## 5. 修复内容

### 5.1 新增诊断脚本

**文件**：`scripts/diagnose_s115_s116_prediction_flow.py`（新增）

- 读取 5 个关键 pkl 文件（v159 / round36_final / round36_eval / canonical_full / canonical_eval）
- 对 S115/S116 逐站点、按 scope（test / test_6_19 / test_10_14 / all）汇总统计
- 自动判定情况 A/B/C/D 并给出明确结论
- 输出 `validation/round56_s115_s116_prediction_flow.csv`

### 5.2 修复诊断脚本 scope bug

原 `diagnose_geo_feature_flow.py` 读取的是 `all` scope（含大量夜间数据），导致 scene_v151 的 night 占比高。

**修复**：新脚本优先读取 `test_10_14` scope（评估时段），其次 `test_6_19`，再次 `test`。

### 5.3 新增 GEO5 链路断言（防止再误判）

**文件**：`scripts/posttrain_validation.py`、`scripts/audit_training_pipeline_flow.py`

新增 GEO5 检查规则：
- 对 S115/S116，在 `test` split + 10-14 时段：
  - `scene_v151` 不能全是 night（否则 FAIL）
  - `g_blend_pred` max > 0（否则 FAIL）
  - `power_pred_final` 至少有一些非 0 行（否则 FAIL）

### 5.4 修复 v159 无 split 列的 scope 处理

v159.pkl 没有 `split` 列（时间范围到 2026-02-09，还在未来期间），只能用 hour 划分 scope。
诊断脚本正确区分了有/无 split 列的文件结构。

---

## 6. 增量模式实测耗时

| mode | 是否触发训练 | 耗时 | 实际执行步骤 | 结果 |
|------|:--:|---:|---|:---:|
| `audit-only` | 否 | **20.8s** | Step 12 (6.9s) + Step 13 (13.9s) | 35 项全部 PASS/仅 WARN |
| `dashboard-only` | 否 | **264.6s (4.4min)** | Step 11 (241.2s) + Step 12 (6.4s) + Step 13 (9.6s) | PASS |
| `eval-only` | 否 | **264.8s (4.4min)** | Step 10 (1.5s) + Step 11 (247.6s) + Step 12 (6.4s) + Step 13 (9.2s) | PASS |
| `full --force` | 是 | 未实测 | — | — |

**验证确认**：
- `audit-only`：Step 6 显示 `[SKIP] 分布式功率模型训练`
- `dashboard-only`：Step 6 显示 `[SKIP] 分布式功率模型训练`
- `eval-only`：Step 6 显示 `[SKIP] 分布式功率模型训练`

**Step 11 主要耗时**：包含 6 个子步骤（hourly_nrmse 重算、dashboard 导出、stamp 检查、fresness 检查、regression check），约 4 分钟。

---

## 7. 最终验证结果

### S115/S116 链路（test 10-14）

| 检查项 | S115 | S116 | 结论 |
|--------|:----:|:----:|------|
| scene_v151 test 10-14 | mid/clear_peak/low（非 night） | mid/clear_peak/low（非 night） | **PASS** |
| g_blend_pred max | 828 | 836 | **PASS** |
| power_pred_final 非 0 行 | 610/610 (100%) | 610/610 (100%) | **PASS** |

### posttrain_validation（35 项）

| 状态 | 数量 |
|------|:----:|
| PASS | 32 |
| FAIL | 0 |
| WARN | 3（C9 夜间记录；C16 manifest 时间被 auto-sync 覆盖；GEO4 S116 low confidence） |

### dashboard check（68 站）

| 指标 | 值 |
|------|---|
| PASS | 68/68 |
| FAIL | 0 |
| 最大 pred 误差 | 0.00e+00（容差 1e-09） |
| 最大 actual 误差 | 0.00e+00 |

---

## 8. 是否需要完整重训

**不需要**。

- v159 正常：scene_v151 有白天成分，g_blend_pred > 0
- canonical_full 正常：scene_v151 有白天成分，g_blend_pred > 0，power_pred_final 非 0
- round36_final/round36_eval 与 canonical 完全一致（无回退）

---

## 9. 验收标准检查

| 标准 | 结果 |
|------|:----:|
| 新增 round56_s115_s116_prediction_flow.csv | PASS（69 行） |
| 明确 v159/final_full/final_eval/dashboard 哪一步异常 | PASS（均正常） |
| 不把 S115/S116 all night 误判为正常 | PASS（GEO5 新增断言） |
| 如果 v159 正常，则不完整重训 | PASS（未完整重训） |
| dashboard-only/eval-only/audit-only 均实测 | PASS（三者均实测） |
| dashboard-only/eval-only/audit-only 不触发 Step 6 训练 | PASS（三者均跳过 Step 6） |
| S115/S116 final_full test 10-14 预测不全 0 | PASS（610/610 非 0） |
| posttrain_validation 无 FAIL | PASS（32 PASS，0 FAIL） |
| dashboard check 无 FAIL | PASS（68/68 PASS） |

---

## 10. 后续建议

1. **Round55 的 "all night" 是诊断脚本 scope 误读，不是产物问题**——可以删除 Round55 执行报告中的该误判内容
2. **Step 11 的 4 分钟耗时主要来自 `export_interactive_dashboard_data`**（约 30-60 秒）和 `recompute_hourly_nrmse`（约 30 秒），其余是子脚本 overhead。如果只改可视化，建议将 Step 11 拆分为 `dashboard-export` 和 `dashboard-check` 两个独立步骤
3. **S116 confidence=low** 仍未解决，应在获得甲方精确场区坐标后更新
4. **auto-sync 机制有时会覆盖 manifest 时间戳**（manifest 早于 pkl 5.64h），C16 已降级为 WARN，不影响主流程
