# Cursor 执行方案 Round53：修正 Round52 口径、Manifest 自动化验证与最终产物源头收口

## 目标

Round52 已基本完成工程闭环，但报告和验证逻辑仍有几个口径问题。本轮不继续调模型，只修正工程与说明口径。

需要解决：

1. `bias < 0` 解释错误：当前项目口径下应表示预测偏低，不是预测偏高。
2. `manifest.json` 报告中一边说自动生成，一边验证项仍是 WARN，口径矛盾。
3. 正式产物目前是从 `round36/round46` 历史文件同步到 canonical 文件，源头仍不干净。
4. `final_full` 为 69 站，`eval/dashboard` 为 68 站，需要明确原因并在验证报告中说明。
5. S116 经纬度为低置信度乡镇级近似坐标，必须在报告、manifest、可视化数据说明中明确。

---

## 一、修正 BIAS 口径

全项目统一 BIAS 定义：

```text
BIAS = 预测 - 实际
BIAS% = (预测总电量 - 实际总电量) / 实际总电量 × 100%
```

解释：

```text
BIAS > 0：预测偏高
BIAS < 0：预测偏低
```

请全局搜索并修正错误描述：

```bash
grep -R "负偏差.*预测偏高\\|bias < 0.*预测偏高\\|BIAS.*预测偏高\\|偏差.*预测偏高" -n scripts docs output/pv_pipeline/docs README.md || true
```

重点检查：

```text
docs/Round52_主流程闭环与S115_S116经纬度补齐报告.md
scripts/posttrain_validation.py
scripts/export_interactive_dashboard_data.py
stages/05_visualization/interactive_forecast_dashboard.html
README.md
```

将类似文字：

```text
系统性负偏差（bias < 0，预测偏高）
```

改为：

```text
系统性负偏差（bias < 0，预测偏低）
```

如果代码中 `bias_mw` 实际定义不是 `pred - actual`，则不要只改文字，必须先统一计算公式。

新增验证脚本或在 `posttrain_validation.py` 增加检查：

```python
def validate_bias_definition(df, pred_col="power_pred_final"):
    actual_sum = df["power_mw"].sum()
    pred_sum = df[pred_col].sum()
    expected_bias_mw = pred_sum - actual_sum
    expected_bias_pct = expected_bias_mw / actual_sum * 100 if abs(actual_sum) > 1e-12 else float("nan")
    return expected_bias_mw, expected_bias_pct
```

要求：

- 指标卡、站点表、报告中的 `BIAS` 均使用同一公式。
- tooltip 或说明文字写明：`BIAS < 0 表示预测偏低`。

---

## 二、修正 manifest 自动生成与验证

Round52 报告里出现矛盾：

```text
manifest.json 自动生成
manifest.json 存在（本次手动生成，已通过） WARN
```

本轮必须让 `manifest.json` 由 `scripts/run_full_pipeline.py` 自动生成，并让 `posttrain_validation.py` 将其判为 PASS。

### 1. 修改 run_full_pipeline.py

确保最后一步必定调用：

```python
write_manifest(cfg, output_root)
```

并且在日志中打印：

```text
[OK] manifest written: output/pv_pipeline/manifest.json
```

manifest 至少包含：

```json
{
  "generated_at": "...",
  "pipeline_entry": "scripts/run_full_pipeline.py",
  "config": "configs/pipeline.yaml",
  "final_prediction_column": "power_pred_final",
  "split": {
    "train_start": "...",
    "valid_start": "...",
    "test_start": "2025-09-01",
    "test_end": "2025-12-31"
  },
  "artifacts": {
    "final_full_pkl": "output/pv_pipeline/predictions/distributed_predictions_final_full.pkl",
    "final_eval_pkl": "output/pv_pipeline/predictions/distributed_predictions_final_eval.pkl",
    "hourly_nrmse_csv": "output/pv_pipeline/metrics/hourly_nrmse_consistent.csv",
    "site_metrics_csv": "output/pv_pipeline/metrics/site_metrics_consistent.csv",
    "dashboard_index": "output/pv_pipeline/interactive_dashboard/index.json"
  },
  "geo_overrides": {
    "file": "configs/manual_station_geo_overrides.csv",
    "low_confidence_sites": ["S116"]
  },
  "station_count_note": "final_full may include all stations; final_eval/dashboard include stations with valid test 6-19 evaluation rows"
}
```

### 2. 修改 posttrain_validation.py

将 manifest 检查改为：

```text
C16 PASS manifest.json exists and is generated by run_full_pipeline.py
```

检查内容：

- `output/pv_pipeline/manifest.json` 存在。
- JSON 可解析。
- `pipeline_entry == scripts/run_full_pipeline.py`。
- `final_prediction_column == power_pred_final`。
- manifest 中列出的 artifact 文件都存在。
- manifest 修改时间晚于或等于 final pkl 修改时间。

如果上述任一项不满足，必须 FAIL，不要 WARN。

---

## 三、让 canonical 文件成为正式源头

当前 Round52 是：

```text
tables/distributed_predictions_final_round36.pkl -> predictions/distributed_predictions_final_full.pkl
metrics/round46_hourly_nrmse_consistent.csv -> metrics/hourly_nrmse_consistent.csv
```

本轮改为：

1. 主流程内部最终直接写 canonical 路径。
2. 如需兼容旧脚本，再从 canonical 复制到历史 round 文件，而不是反过来。

正式路径：

```text
output/pv_pipeline/predictions/distributed_predictions_final_full.pkl
output/pv_pipeline/predictions/distributed_predictions_final_eval.pkl
output/pv_pipeline/metrics/hourly_nrmse_consistent.csv
output/pv_pipeline/metrics/site_metrics_consistent.csv
```

兼容路径可选：

```text
output/pv_pipeline/tables/distributed_predictions_final_round36.pkl
output/pv_pipeline/tables/distributed_predictions_final_eval_round36.pkl
output/pv_pipeline/metrics/round46_hourly_nrmse_consistent.csv
output/pv_pipeline/metrics/round36_site_metrics.csv
```

请修改：

```text
scripts/run_full_pipeline.py
scripts/export_interactive_dashboard_data.py
scripts/posttrain_validation.py
scripts/check_dashboard_prediction_values.py
```

要求：

- 评估脚本只读取 canonical 路径。
- 可视化导出只读取 canonical 路径。
- README 只说明 canonical 路径。
- 旧 round 文件只用于兼容，不再作为正式输入。

验证：

```bash
grep -R "distributed_predictions_final_round36\\|round46_hourly\\|round36_site_metrics" -n scripts README.md docs | grep -v archive || true
```

除了兼容复制函数和历史报告外，主流程中不应再依赖这些文件。

---

## 四、明确 69 站 / 68 站差异

在 `posttrain_validation.py` 和报告中增加说明：

```text
final_full 保留全量站点和全量时间行；
final_eval/dashboard 仅保留参与测试集 6-19 点评估且有有效记录的站点；
因此 final_full 站点数可能大于 eval/dashboard 站点数。
```

新增检查：

```python
full_station_count = full_df["station_id"].nunique()
eval_station_count = eval_df["station_id"].nunique()
if full_station_count < eval_station_count:
    FAIL
elif full_station_count > eval_station_count:
    PASS_WITH_NOTE
else:
    PASS
```

输出被排除站点清单：

```text
output/pv_pipeline/validation/excluded_from_eval_sites.csv
```

字段：

```text
station_id
station_name
capacity_mw
reason
full_rows
test_6_19_rows
positive_test_6_19_rows
zero_ratio_test_6_19
```

如果 dashboard 是 68 站，也要说明它与 eval 口径一致。

---

## 五、强化 S116 低置信度说明

S116 当前坐标：

```text
station_id: S116
latitude: 34.2983
longitude: 119.2318
confidence: low
source: manual_wiki_town_center
```

这个坐标只能作为临时近似坐标，不是光伏场区中心。

请在以下位置加入一致说明：

1. `configs/manual_station_geo_overrides.csv` 的 `note`
2. `manifest.json` 的 `geo_overrides.low_confidence_sites`
3. `posttrain_validation_report.md` 的 GEO4 WARN
4. `docs/Round53_口径修正与Manifest自动化验证报告.md`
5. 可视化导出的站点 metadata 中，如果有 `geo_confidence` 字段，保留 `low`

说明文字建议：

```text
S116 当前使用伊山镇级近似坐标，置信度 low，仅用于避免空间特征缺失；正式应用前应由甲方或运维台账确认光伏场区中心坐标。
```

不要在正式报告中把 S116 坐标描述为精确坐标。

---

## 六、重新执行主流程验证

执行：

```bash
cd /home/ac/data16t/msc/photovoltaic_forecasting_pj

python scripts/run_full_pipeline.py --config configs/pipeline.yaml 2>&1 | tee output/pv_pipeline/logs/round53_full_pipeline.log

python scripts/posttrain_validation.py --config configs/pipeline.yaml 2>&1 | tee output/pv_pipeline/logs/round53_posttrain_validation.log

python scripts/check_dashboard_prediction_values.py --config configs/pipeline.yaml 2>&1 | tee output/pv_pipeline/logs/round53_dashboard_check.log
```

检查：

```bash
grep -Ei "FAIL|ERROR|Traceback|Exception|FileNotFound|KeyError|ValueError" \
  output/pv_pipeline/logs/round53_full_pipeline.log \
  output/pv_pipeline/logs/round53_posttrain_validation.log \
  output/pv_pipeline/logs/round53_dashboard_check.log || true
```

检查 manifest：

```bash
python -m json.tool output/pv_pipeline/manifest.json | sed -n '1,160p'
```

检查 canonical 文件：

```bash
ls -lh \
  output/pv_pipeline/predictions/distributed_predictions_final_full.pkl \
  output/pv_pipeline/predictions/distributed_predictions_final_eval.pkl \
  output/pv_pipeline/metrics/hourly_nrmse_consistent.csv \
  output/pv_pipeline/metrics/site_metrics_consistent.csv \
  output/pv_pipeline/interactive_dashboard/index.json \
  output/pv_pipeline/manifest.json
```

---

## 七、专项核对 BIAS

执行：

```bash
python - <<'PY'
import pandas as pd
from pathlib import Path

path = Path("output/pv_pipeline/predictions/distributed_predictions_final_eval.pkl")
df = pd.read_pickle(path)
pred_col = "power_pred_final"

actual_sum = df["power_mw"].sum()
pred_sum = df[pred_col].sum()
bias_mw = pred_sum - actual_sum
bias_pct = bias_mw / actual_sum * 100
ratio = pred_sum / actual_sum

print("actual_sum_mwh_like:", actual_sum)
print("pred_sum_mwh_like:", pred_sum)
print("pred/actual:", ratio)
print("bias_mw_like:", bias_mw)
print("bias_pct:", bias_pct)
print("interpretation:", "预测偏高" if bias_mw > 0 else "预测偏低" if bias_mw < 0 else "无总体偏差")

for sid in ["S115", "S116"]:
    sdf = df[df["station_id"].eq(sid)]
    if sdf.empty:
        print(sid, "not in eval")
        continue
    a = sdf["power_mw"].sum()
    p = sdf[pred_col].sum()
    b = p - a
    print(sid, "actual", a, "pred", p, "bias", b, "bias_pct", b / a * 100 if a else None,
          "interpretation", "预测偏高" if b > 0 else "预测偏低" if b < 0 else "无总体偏差")
PY
```

要求：

- 输出解释与报告一致。
- 如果 S115/S116 是负偏差，应写预测偏低。

---

## 八、生成 Round53 报告

新增：

```text
docs/Round53_口径修正与Manifest自动化验证报告.md
```

模板：

```markdown
# Round53 口径修正与 Manifest 自动化验证报告

## 1. 本轮修改目标

## 2. BIAS 口径修正

BIAS = 预测 - 实际；BIAS < 0 表示预测偏低，BIAS > 0 表示预测偏高。

## 3. Manifest 自动生成验证

| 检查项 | 结果 |
|---|---|
| manifest 是否自动生成 |  |
| 是否由 run_full_pipeline.py 写出 |  |
| 是否包含 final_prediction_column |  |
| artifact 文件是否存在 |  |
| 修改时间是否晚于 final pkl |  |

## 4. Canonical 文件源头收口

## 5. 69 站 / 68 站差异说明

## 6. S116 低置信度坐标说明

## 7. posttrain_validation 结果

## 8. dashboard 校验结果

## 9. 当前仍需注意的问题
```

---

## 九、验收标准

本轮必须满足：

```text
[PASS] BIAS 公式和解释全项目统一
[PASS] bias < 0 不再被写成预测偏高
[PASS] manifest.json 由 run_full_pipeline.py 自动生成
[PASS] posttrain_validation 中 manifest 检查为 PASS，不再是 WARN
[PASS] 正式评估和可视化读取 canonical 文件
[PASS] 历史 round 文件不再作为正式输入
[PASS] 69/68 站差异有 excluded_from_eval_sites.csv 说明
[PASS] S116 low confidence 在 manifest/report/metadata 中均有说明
[PASS] posttrain_validation 无 FAIL
[PASS] dashboard check 无 FAIL
```

